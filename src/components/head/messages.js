import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  'orders.page.title': {
    id: 'orders.page.title',
    defaultMessage: 'Order History | {siteName}',
    description: 'Title tag',
  },
});

export default messages;
